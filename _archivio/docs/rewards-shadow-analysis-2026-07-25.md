# Shadow-log analysis — rewards drift + maker audit

Read-only analysis of two logs that had not been read. No surface changed; nothing was placed.

## 1. `data/rewards-drift-shadow.jsonl`

**158 rows**, span 64.3h (2026-07-22T21:23:26.553Z → 2026-07-25T13:43:40.331Z), **5 distinct markets**.

What it is: a **drift-advisory** log from the DISARMED shadow maker. Every row is `decision:"drift"`, 
`executed:false`, `mode:"shadow"` — it is written ONLY when a resting config has drifted past its band 
radius (or left the band), advising a manual re-quote. It is therefore **event-triggered, not a uniform 
time sampler**: the rows are, by construction, biased toward out-of-band moments.

### In-band fraction (Q1) — the data cannot support a trustworthy figure

- At the advisory instants, `inBandNow` was true in only **9/158 (5.7%)** — but this is a 
  lower-bound artifact of the event trigger (the advisory fires *because* the config drifted), **not** the real in-band fraction.
- The `measured.inBandSec` / `outBandSec` fields are per-session accumulators that **reset** (peaks reach ~44h while the
  trailing value drops back to 0), so they cannot be composed into a reliable time fraction either.
- Per-market row counts are dominated by a single market:
  - `0xcd264046…` — 112 rows, inBandNow 3/112 (2.7%), band radius 2.25c
  - `0x3849e1d6…` — 23 rows, inBandNow 0/23 (0.0%), band radius 2.25c
  - `0x5db999fa…` — 18 rows, inBandNow 4/18 (22.2%), band radius 1.75c
  - `0x684e5b72…` — 4 rows, inBandNow 2/4 (50.0%), band radius 2.25c
  - `0xe6bcc2f1…` — 1 rows, inBandNow 0/1 (0.0%), band radius 2.75c

> **Verdict on Q1:** too small and too biased to state a real in-band fraction. One market (`0xcd264046…`) is 70.9% of all rows, 
> and the log only fires on drift. The honest answer is that this log measures *how often a re-quote was advised*, not *what fraction of time the book was in band*.

### Drift-advisory reasons, ranked (Q2, drift signal)

- **134** (84.8%) — drift>=radius(2.25c) — advisory: re-quote by hand (disarmed, no order placed)
- **17** (10.8%) — drift>=radius(1.75c) — advisory: re-quote by hand (disarmed, no order placed)
- **6** (3.8%) — left-band — advisory: re-quote by hand (disarmed, no order placed)
- **1** (0.6%) — drift>=radius(2.75c) — advisory: re-quote by hand (disarmed, no order placed)

## 2. `data/polymarket-maker-audit.jsonl`

**683 rows**, span 66.6h (2026-07-22T19:37:02.309Z → 2026-07-25T14:12:36.221Z).

What it is: the maker adapter's decision audit across operations. Row `op` breakdown:
- `postOrder`: 444
- `plan`: 84
- `listOpenOrders`: 77
- `cancelMarketOrders`: 74
- `close`: 4

**Contamination note:** the `postOrder` rows carry test-harness modes (`live-min`, `live:dryrun`, `live`) — those are
`scripts/maker-selfcheck.js` invocations, NOT production. The **decision analysis below is restricted to the `plan`**
**rows, which are ALL `mode:"paper"`** — i.e. real disarmed agent35 paper-cycle planning.

### Decisions & declined-to-quote reasons (Q2, maker signal)

- **84 paper plan decisions** across **8 markets**.
- **Quoted** (wouldPost ≥ 1): 69/84 (82.1%).
- **Declined to quote** (wouldPost = 0): **15/84 (17.9%)**.
- Decline reasons, ranked:
  - **15/15 (100.0%)** — feed not live (`feedLive:false` → `railHalt:"market"` → no quote).
- **Correlation:** with a LIVE feed, the maker declined **0/69** times. Every decline coincided with a non-live feed.
- Quote sizes when quoting: post=2×23, post=1×23, post=3×20, post=16×3.
- Two-sided vs one-sided plans: 46 two-sided / 38 one-sided; `oneSidedPenalty` was true in 0.

## 3. Do the reasons cluster on anything actionable? (Q3)

- **Yes, cleanly, for declines:** 100% of the 15 paper-plan declines were the non-live-feed rail halt, and a live feed
  *always* produced a quote. That is exactly the honest-engine "no quote on a stale feed" rule firing — actionable and expected,
  and it says feed liveness is the sole gate on whether the paper maker quotes at all.
- **Drift advisories** cluster on the band-radius breach (the 2.25c radius dominates), but 71% of the drift rows come from a single
  market, so that is one market's behaviour, not a fleet pattern.

### Where the sample is too small to conclude

- 84 paper plan decisions over 67h across 8 markets, with only 15 declines — enough to say *why* it declined,
  not enough to estimate a decline *rate* that would generalise.
- 158 drift rows over 64h across 5 markets, one market dominating — **do not extrapolate** an in-band fraction from it.
- No P&L conclusion is possible from either file: both are pre-execution planning/advisory logs (`executed:false`), not fills.

_Generated by `scripts/rewards-replay/analyze-shadow-logs.js` (read-only)._
